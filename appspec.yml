version: 0.0
os: centOS
files:
  - source: /
    destination: /var/www/html/
hooks:
  AfterInstall:
   - location: setup.sh
     timeout: 300
     runas: root
  ApplicationStart:
   - location: run.sh
     timeout: 300
     runas: root
